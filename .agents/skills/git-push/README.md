# git-push スキル

`/git-push` で、`add -> commit -> push` を行うスキルです（PR作成はしません）。

- 既定は unstaged を含めて自動で `git add -A`
- `main` / `master` では分岐先を選んでから継続
- コミットメッセージは推奨1件を自動判断して実行

詳細ルールは [SKILL.md](./SKILL.md) を参照してください。
